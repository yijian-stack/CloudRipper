# 任务交接说明

请基于下面的固定数据路径和目标，直接生成一份**可运行的完整代码**。用户会把你的完整代码直接放到本机运行，并用结果和其他 AI 的代码做对比。

## 1. 固定数据路径

- 训练集目录固定为：
  - `D:\company\0125\门机\datarec\grabmix_train15_test15`
- 独立测试/验证目录固定为：
  - `D:\company\0125\门机\datarec\grabtest`

不要改这两个路径，不要让用户再传路径。

## 2. 数据格式

每个文件都是 `csv`，**无表头**。

- 训练/测试输入列格式：
  - `x,y,z,nx,ny,nz,label`
- 其中：
  - `x,y,z` 是点坐标
  - `nx,ny,nz` 是法向量，已经预先算好
  - `label` 是实例分割标签

标签语义：

- `0` 表示背景
- 非零表示目标
- 不同的非零值表示不同实例
- 但本质上只有一类目标，所以请优先把它当成**目标识别 / 前景目标检测**任务来做，而不是执着于通用实例分割 benchmark

## 3. 最终任务目标

目标不是追求常规语义分割指标，而是：

- 尽可能稳定地识别出目标
- 尽量不要把背景、构件、环境结构误识别成目标
- 尽量不要只识别到目标的一部分
- 尽量不要把目标附近的背景吞进去
- 多目标场景下也要尽量把目标找全

用户更看重这些指标：

- 目标级 `precision / recall / f1`
- 目标完整度
- 与真值目标包围盒尺寸的接近程度
- 与真值目标包围盒中心/尺度的一致性

因为背景点极多，整体点级 accuracy 很高没有意义。

## 4. 输出格式要求

最终测试集预测结果必须导出成 `csv`，**无表头**，格式固定为：

- `x,y,z,pred_label`

要求：

- `x,y,z` 保持原场景原始坐标
- `pred_label` 为预测标签
- 用户以后可能只保留目标信息，所以这部分“从模型输出映射回整个场景 csv”的后处理请单独写清楚、模块化，方便以后删改

## 5. 对模型的要求

- 网络结构不限
- 不要受当前代码里旧版 U-Net 设计限制
- 可以大胆重构网络
- 但请优先考虑：
  - 点云目标识别能力
  - 环境理解能力
  - 推理速度
  - 显存稳定性
- 请尽量避免“主要靠传统后处理硬调出来”的方案
- 主体应以深度学习识别能力为主

## 6. 已知有效的输入预处理思路

这些方向是用户认可的，可以保留或改进：

- 坐标单位归一化
  - 不管原始单位是米还是毫米，都尽量做自适应归一化
- 预下采样 / 体素稀疏
  - 例如 `0.1` 体素中只保留一个点
  - 目的是提速
- 法向量是有用特征
  - 不要忽略 `nx,ny,nz`
- 上下方向基本固定
  - 不建议上下翻转增强
- 旋转增强可做
- 可以做背景删除/背景 dropout
  - 但绝对不能删掉目标

## 7. 已经踩过的主要坑

请尽量避免重复踩这些坑。

### 坑 1：训练采样太偏目标，导致模型把大面积背景都报成目标

之前有一版训练配置大致是：

- `positive_fraction=0.75`
- `hard_negative_fraction=0.20`

这会导致：

- 训练 crop 太偏目标
- 纯背景环境学得不够
- 模型在整场上容易把大面积背景都当成目标

真实出现过的现象：

- 某些验证场景，真值前景只有 `1000~2000` 点
- 模型却能报出 `7万~11万` 个前景点

### 坑 2：loss 太偏召回，不够惩罚误检

之前有一版使用了：

- `focal_alpha=0.75`
- `tversky_alpha=0.30`
- `tversky_beta=0.70`

这类配置更偏“宁可多报也别漏报”，在当前任务里会明显导致：

- 误检背景
- 背景构件被吞进目标
- 目标附近环境被误识别成目标

### 坑 3：验证逻辑里阈值 sweep 不可信

之前有一版训练期验证时：

- 仍然开着 `adaptive_threshold`
- 日志里虽然写着在 sweep `thr=0.1/0.2/0.3...`
- 但阈值实际上没真正起作用

结果：

- `best_thr`、`last score` 看起来很怪
- 很难判断模型到底有没有变好

如果你要做验证/选模，请确保：

- 训练期验证时阈值 sweep 真的有效
- 不要让 adaptive threshold 把 sweep 覆盖掉
- 最终后处理可以单独校准

### 坑 4：整场验证太慢，尤其首轮经常卡住

现在还有一个很现实的问题：

- 用户认为**但凡一个 epoch 超过 40 秒都不正常**

之前出现过“epoch1 卡二十多分钟”的情况，主要原因不是训练 step 本身，而是：

- 首轮 CUDA / spconv 编译开销
- 第 1 个 epoch 结束后立刻做整场验证
- 整场验证本身很耗时

也就是说：

- 非验证 epoch 往往是几十秒
- 但如果把整场验证时间算进 epoch，看起来就会像“一个 epoch 十几二十分钟”

请尽量：

- 不要在第 1 个 epoch 就强制整场验证
- 明确区分训练时间和验证时间
- 控制整场验证频率
- 或设计更快的验证方案

### 坑 5：spconv 稀疏卷积在大场景上仍可能 OOM

之前的代码里，曾经尝试更深的 sparse UNet / dilated sparse block。

实际现象：

- 即使用的是稀疏卷积
- 在整场大点云验证时仍然可能 OOM
- 尤其是 stride sparse conv、深层 sparse stage、dilated SubMConv 等

已经确认：

- 不能简单假设“稀疏卷积一定很省显存”
- 大场景整场验证仍然要考虑极端场景的显存稳定性

如果你设计网络，请兼顾：

- 千万级/超大场景的推理稳定性
- 速度
- 显存

## 8. 当前已经验证过的现象

下面这些结论是已经跑出来过的，不是猜测。

### 现象 A：旧的偏目标采样 + 偏召回 loss 会把背景吞成目标

曾经有一个 best checkpoint，在独立 `grabtest` 上：

- `score` 只有大约 `0.19`
- `fg_iou` 只有大约 `0.03`
- 每个场景的平均 `score_prob` 约 `0.19~0.23`
- 大量背景点被当成目标

### 现象 B：修正采样 / loss / 验证逻辑后，短诊断已经明显改善

曾经做过一个短诊断 run，对应 checkpoint：

- `D:\writing\company\pointnet\targetedRecognition\0320\tapa_spvlike_diag5_best.pth`

这版在独立 `grabtest` 上，使用更合理的验证逻辑后，拿到过：

- `score ≈ 0.6959`
- `obj_f1 ≈ 0.7222`
- `fg_iou ≈ 0.5949`
- `bbox_score ≈ 0.6318`

这说明：

- 之前的问题很大一部分不是“网络完全无效”
- 而是训练采样、loss 偏向、验证逻辑这些地方有明显问题

但这版还不够好，距离工业可用还远，仍需要继续优化。

## 9. 对其他 AI 的明确要求

请直接输出一份**完整、可运行、可训练、可预测**的代码，要求：

- 固定读取上面的训练/测试目录
- 支持训练
- 支持在 `grabtest` 上预测
- 支持输出无表头 `x,y,z,pred_label`
- 训练后能直接用测试集真值评估效果
- 代码里请把“训练 / 预测 / 后处理 / 导出 csv”写清楚

重点不是复刻当前代码，而是：

- 设计一个更强、也更稳的点云目标识别方案
- 识别效果尽可能好
- 速度不能太慢
- 不能老是卡在首轮或大场景验证

## 10. 当前用户最在意的点

- 目标识别效果要明显好
- 环境理解要更强
- 少误检、少漏检
- 多目标场景也要处理好
- 训练和验证速度不能离谱
- 生成的代码应当是“完整代码”，用户可以直接拿去运行


保证全程可在wins系统运行，可以适用几十万点云的量级，预测阶段的速度可以达到秒级。